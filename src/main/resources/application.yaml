spring:
  application:
    name: booking-service
  server:
    port: 8080 

  datasource:
    url: jdbc:postgresql://database-1.cbe6s0ymsaw0.ap-south-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: Satish89babu#

  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

  endpoint:
    health:
      show-details: always

  metrics:
    export:
      prometheus:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs